<h1><%= @post.title %></h1>
<h4><%= @post.body %></h4>

<%= link_to "Edit", edit_post_path, class: 'btn btn-default' if policy(@post).edit? %>
<%#= link_to "Edit", edit_post_path, class: 'btn btn-default' if policy(Post.find(params[:id])).edit? %>

