<h4>Add a Comments</h4>

<%= form_for [@topic, @post, @post.comments.new] do |f|  %>
	
	<div class="form-group">
		<%#= f.label :body %>
		<%= f.text_area :body, rows: 5, class: "form-control", placeholder: "Enter your commnent" %>
	</div>

	<div class="form-group">
		<%= f.submit "save", class: 'btn btn-default' %>
	</div>

<% end %> 
